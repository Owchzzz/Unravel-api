using ServerManager.Connection;
namespace ServerManager.Authenticated {

    class Authenticated {
        private String Token;
        public Authenticated(String token) {
            Token = token;
        }
    }
}